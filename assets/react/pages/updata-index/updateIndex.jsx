/** 更新搜索索引* */import React from 'react';class UpdateIndex extends React.Component {    constructor(props) {        super(props);    }    updateIndex() {        H.server.search_index_init({}, (res) => {            if(res.code == 0) {                H.Modal('更新索引成功');            }else if(res.code == 10106) {                H.overdue();            }else {                H.Modal(res.message);            }        });    }    render() {        return (            <div className="section-warp">                <div className="section-filter">                    <form className="form-inline">                        <div className="filter-row">                             <btn className="btn btn-lg" onClick={this.updateIndex.bind(this)} >更新索引</btn>                        </div>                    </form>                </div>            </div>        );    }}export default UpdateIndex;